
// no se sabbe que hace pero la profesora lo tenia :d


import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary:{
            main:'#39A900',
        },
        secondary:{
            main: '#007832',
        },
    },
});

export default theme;